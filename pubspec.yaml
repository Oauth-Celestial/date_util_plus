name: date_util_plus
description: DateUtilPlus, a powerful Dart API designed to augment and simplify date and time handling in your Dart projects. 
version: 0.0.4
repository: "https://github.com/Oauth-Celestial/date_util_plus.git"

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:


